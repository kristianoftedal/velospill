---
phase: 07-orders-polish-integration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scoring-queries.ts
  - src/lib/order-queries.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Kaptein country_all variant applies x1.5 to all own riders from the targeted country in a World Championship"
    - "riderNationality is populated in baseScores used by applyOrderEffects"
    - "Shimanobil counter fires only when the rider's owning team has a defense order, not any arbitrary third team"
  artifacts:
    - path: "src/lib/scoring-queries.ts"
      provides: "RaceScoreEntry type with riderNationality field; getRaceScoreBreakdown selects riders.nationality"
      contains: "riderNationality"
    - path: "src/lib/order-queries.ts"
      provides: "baseScores construction includes riderNationality in both getOrderAdjustedStandings and getRaceScoreBreakdownWithOrders"
      contains: "riderNationality: entry.riderNationality"
  key_links:
    - from: "src/lib/order-queries.ts applyOrderEffects"
      to: "baseScores[].riderNationality"
      via: "s.riderNationality === order.targetCountry comparison"
      pattern: "riderNationality.*order\\.targetCountry"
---

<objective>
Close the Kaptein country_all gap identified in phase 07 verification.

Purpose: The `applyOrderEffects` function compares `s.riderNationality === order.targetCountry` to award the x1.5 multiplier to all own riders from a targeted country in a World Championship. This comparison always evaluates to `undefined !== string` because `riderNationality` is never selected from the database and never propagated into the `baseScores` array. The fix adds `nationality` to the `getRaceScoreBreakdown` select, propagates it through `RaceScoreEntry` and both `baseScores` construction sites, and tightens the Shimanobil counter check.
Output: Kaptein country_all variant correctly applies x1.5 to matching riders.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-orders-polish-integration/07-04-SUMMARY.md
@src/lib/scoring-queries.ts
@src/lib/order-queries.ts
@src/db/schema/riders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add riderNationality to getRaceScoreBreakdown and RaceScoreEntry</name>
  <files>src/lib/scoring-queries.ts</files>
  <action>
    In `getRaceScoreBreakdown` (line 141), add `nationality: riders.nationality` to the `.select()` object alongside `riderTeam: riders.team`. The `riders` table is already joined via `innerJoin(riders, eq(riders.id, raceResults.riderId))` so no join change is needed.

    In the `.map()` return (lines 167-176), add `riderNationality: row.nationality` to the mapped object so the field is included in returned rows.

    In the `RaceScoreEntry` type (lines 217-225), add `riderNationality: string` as a required field.

    In `getRaceScoreBreakdownWithOrders` (around line 349), where `baseScores` is constructed from `baseEntries`, add `riderNationality: entry.riderNationality` to the BaseScore object. The local `BaseScore` type defined at line 348 must also include `riderNationality?: string` (optional, to match the `BaseScore` type in order-queries.ts which already declares `riderNationality?: string`).

    Do NOT modify `getLeagueStandings`, `getTeamRiderScores`, `getLeagueRacesWithScores`, or any other function — only the four changes above.
  </action>
  <verify>Run `npx tsc --noEmit` from the project root. Expect zero new TypeScript errors. Grep confirms: `riderNationality` appears in scoring-queries.ts select, map, type, and baseScores construction.</verify>
  <done>
    `getRaceScoreBreakdown` selects `nationality: riders.nationality`, maps it to `riderNationality` in returned rows, `RaceScoreEntry` type includes `riderNationality: string`, and `getRaceScoreBreakdownWithOrders` baseScores construction includes `riderNationality: entry.riderNationality`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Propagate riderNationality into getOrderAdjustedStandings baseScores and tighten Shimanobil counter check</name>
  <files>src/lib/order-queries.ts</files>
  <action>
    In `getOrderAdjustedStandings` (around line 635), the `baseScores` array is built from `breakdown` (the result of `getRaceScoreBreakdown`). Add `riderNationality: entry.riderNationality` to the `BaseScore` object alongside `position: entry.position`. The `BaseScore` type at line 191 already has `riderNationality?: string` — no type change needed there.

    For the Shimanobil counter fix in `resolveCounters` (lines 143-161): the current check `defenseOrders.find((d) => d.teamId !== attack.teamId)` matches ANY team that has a defense order, not specifically the team that owns the targeted rider. To fix this: the Shimanobil attack has `targetRiderId` but no `targetTeamId`. We need to find which team in the league owns that rider. Since `activeOrders` does not carry ownership info, use a pragmatic fix: instead of matching `d.teamId !== attack.teamId`, match the defense order against the teams that have drafted riders listed in the active orders' context. The simplest correct approach within the pure function: require the defense order's team to own the targeted rider by passing ownership context — but that would change the function signature.

    Acceptable in-function approach (minimal change, correct for the common case): replace `d.teamId !== attack.teamId` with a check that looks for a defense order whose `teamId` matches one of the non-attacker teams that submitted ANY active order for this race. This is still not perfect but is the closest we can get without a DB query in a pure function.

    Actually, the cleanest minimal fix: in the `targetedTeamId == null` branch, skip the counter check entirely (leave `defenseForTargetedTeam` undefined) when there is no way to determine the rider's owning team from the available data. Add a TODO comment: `// TODO: Shimanobil counter requires rider ownership lookup — cannot resolve in pure function without targetTeamId`. This is a no-op change that documents the limitation without introducing incorrect behavior. The counter will simply not fire for Shimanobil when `targetTeamId` is null, which is safer than incorrectly firing it.

    Note: The verification warning says "fix if easy" — the pure-function limitation means the correct fix requires either (a) adding a targetTeamId to Shimanobil orders at submission time or (b) changing resolveCounters to be async. Neither is trivial. Implement the documentation-only approach (add the TODO comment) to make the limitation explicit without breaking the existing partial behavior.

    If the TODO-only approach feels insufficient, alternative minimal fix: change the `defenseOrders.find` to require that the defense order's team is NOT the attacker AND is one of the teams in `activeOrders` that has `targetTeamId === null` and `targetRiderId === attack.targetRiderId` — meaning a team that defended specifically against this rider. This requires a Shimanobil defense to target the same rider, which is not how the current order model works. Stick with the TODO comment approach.
  </action>
  <verify>Run `npx tsc --noEmit`. Expect zero TypeScript errors. Confirm `riderNationality: entry.riderNationality` appears in getOrderAdjustedStandings baseScores construction. Confirm the Shimanobil counter branch has the TODO comment.</verify>
  <done>
    `getOrderAdjustedStandings` baseScores construction includes `riderNationality: entry.riderNationality`. The Shimanobil counter branch in `resolveCounters` has a TODO comment documenting the ownership-lookup limitation. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` passes with zero errors
2. Grep `riderNationality` in `src/lib/scoring-queries.ts` shows occurrences in: select statement, map return, RaceScoreEntry type, and baseScores construction in `getRaceScoreBreakdownWithOrders`
3. Grep `riderNationality` in `src/lib/order-queries.ts` shows occurrence in baseScores construction in `getOrderAdjustedStandings`
4. The `country_all` branch in `applyOrderEffects` (order-queries.ts ~line 381) will now receive populated `riderNationality` values and correctly match riders by nationality
</verification>

<success_criteria>
- `RaceScoreEntry` type exported from `scoring-queries.ts` includes `riderNationality: string`
- `getRaceScoreBreakdown` selects `nationality: riders.nationality` and maps it to `riderNationality`
- `getOrderAdjustedStandings` baseScores includes `riderNationality: entry.riderNationality`
- `getRaceScoreBreakdownWithOrders` baseScores includes `riderNationality: entry.riderNationality`
- TypeScript compiles with zero errors
- Kaptein country_all variant will now find matching riders when `s.riderNationality === order.targetCountry` is evaluated in `applyOrderEffects`
</success_criteria>

<output>
After completion, create `.planning/phases/07-orders-polish-integration/07-05-SUMMARY.md`
</output>
