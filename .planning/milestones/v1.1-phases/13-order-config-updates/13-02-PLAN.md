---
phase: 13-order-config-updates
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/lib/order-queries.ts
  - src/app/(main)/leagues/[leagueId]/orders/orders-client.tsx
autonomous: true
requirements:
  - ORDER-01
  - ORDER-02
  - ORDER-05
  - ORDER-08

must_haves:
  truths:
    - "Blodpose GT correctly applies x3 for TdF races and x3.5 for Giro/Vuelta races"
    - "Etappeseier multiplies all own riders finish points by race-specific multiplier (not just top-10)"
    - "Sponsorens ritt multiplies end-of-tour points by 3x instead of 2x"
    - "Kaptein order can be submitted and scored for women's one-day races"
    - "Admin can select and submit all updated orders with correct multipliers via UI"
  artifacts:
    - path: "src/lib/order-queries.ts"
      provides: "Updated applyOrderEffects with new effect type handlers"
      contains: "multiply_finish_points"
    - path: "src/app/(main)/leagues/[leagueId]/orders/orders-client.tsx"
      provides: "Updated UI supporting women's WC kaptein"
  key_links:
    - from: "src/lib/order-queries.ts"
      to: "orderTypes.effect JSONB"
      via: "effectType switch statement"
      pattern: "case .multiply_finish_points."
    - from: "src/lib/order-queries.ts"
      to: "orderTypes.effect JSONB"
      via: "effectType switch statement"
      pattern: "case .multiply_end_tour."
---

<objective>
Update order effect application logic in order-queries.ts to handle new effect types (multiply_finish_points, multiply_end_tour) and remove the World Championship gate from Kaptein.

Purpose: The migration in Plan 01 changes the orderTypes JSONB effect structures. The application code must be updated to handle the new effect types and per-GT multiplier values so that scoring correctly reflects the 2026 order rules.

Output: Updated order-queries.ts with new switch cases, updated orders-client.tsx for Kaptein on women's races.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-order-config-updates/13-01-SUMMARY.md
@src/lib/order-queries.ts
@src/app/(main)/leagues/[leagueId]/orders/orders-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update applyOrderEffects for new effect types and Blodpose GT</name>
  <files>src/lib/order-queries.ts</files>
  <action>
Modify the `applyOrderEffects` function in `src/lib/order-queries.ts` with these specific changes:

**1. Blodpose GT multiplier resolution (ORDER-01):**
The existing `case "multiplier"` code already handles `effectValues[raceType]` with fallback to `effectValue`. After Plan 01 migration, blodpose_gt will have `values: {grand_tour: 3.5, grand_tour_tdf: 3}` instead of `value: 3`. The existing code path already works for this, but there is a subtle issue: for stages, the `raceType` passed is the stage's race type. For GT stages, the raceType is `grand_tour` or `grand_tour_tdf` from the parent. Verify the `getOrderAdjustedStandings` function passes the correct raceType (it reads `races.raceType` which for stages will be the parent race type because orders are submitted against parent races). No code change needed for ORDER-01 -- the existing multiplier code path handles it. Add a comment noting this.

**2. New `multiply_finish_points` effect type (ORDER-02 -- Etappeseier):**
Replace the existing `case "double_top10_stage"` with a new `case "multiply_finish_points"`. The new logic:
- Get the per-race-type multiplier from `order.effectValues?.[raceType] ?? 2` (default 2 for safety)
- Filter `baseScores` for riders on the order's team (`s.teamId === order.teamId`)
- For each rider with `points > 0`, create an adjustment with `adjustedPoints: Math.floor(entry.points * multiplier)`
- Description: `${order.orderTypeName} x${multiplier} (finish pts)`
- This applies to ALL own riders' finish points (not limited to top-10 position), which is the key difference from the old `double_top10_stage`

**3. New `multiply_end_tour` effect type (ORDER-05 -- Sponsorens ritt):**
Replace the existing `case "double_end_tour"` with `case "multiply_end_tour"`. The new logic:
- Get multiplier from `order.effectValue ?? 3` (default 3)
- Filter `baseScores` for riders on the order's team
- For each rider with `points > 0`, create an adjustment with `adjustedPoints: Math.floor(entry.points * multiplier)`
- Description: `${order.orderTypeName} x${multiplier}`

**4. Remove World Championship gate from `choice` case (ORDER-08 -- Kaptein):**
In the `case "choice"` block, remove the `if (!isWorldChampionship) break` guard. The Kaptein order should now work for any race type where it's configured as applicable (WC and women's one-day). The applicableRaceTypes check already happens at order submission time in actions.ts. Also remove the comment "kaptein -- only applies in World Championship".

**5. Update World Championship skip logic at top of loop:**
Currently the code has `if (isWorldChampionship && order.orderTypeName !== "kaptein") continue`. This is still correct -- for WC races, only kaptein applies. But now kaptein ALSO applies to non-WC races. No change needed here, just verify it works both ways.

**6. Update the JSDoc comment** for `applyOrderEffects` to reflect the new effect types:
- Replace `double_top10_stage: etappeseier` with `multiply_finish_points: etappeseier`
- Replace `double_end_tour: sponsorens_ritt` with `multiply_end_tour: sponsorens_ritt`

Also update the `DEFENSE_ORDER_NAMES` array comment if needed -- etappeseier is still a defense order, the name hasn't changed.
  </action>
  <verify>
Read `src/lib/order-queries.ts` and confirm:
1. `case "multiply_finish_points"` exists with per-race-type multiplier logic
2. `case "multiply_end_tour"` exists with configurable multiplier
3. No `case "double_top10_stage"` or `case "double_end_tour"` remain
4. `case "choice"` has no `isWorldChampionship` guard
5. JSDoc reflects new effect type names
  </verify>
  <done>applyOrderEffects handles multiply_finish_points (Etappeseier), multiply_end_tour (Sponsorens ritt), and Kaptein without WC restriction.</done>
</task>

<task type="auto">
  <name>Task 2: Update orders client UI for women's WC Kaptein support</name>
  <files>src/app/(main)/leagues/[leagueId]/orders/orders-client.tsx</files>
  <action>
The orders-client.tsx reads effect target types from the DB dynamically, so the Kaptein UI (`own_rider_or_country` target) already renders for any order type that has that target. Since Plan 01 added `womens_one_day` to kaptein's `applicableRaceTypes`, the UI will automatically show kaptein as an option when a women's one-day race is selected.

However, verify one edge case: the `effectiveRaceType` calculation in orders-client.tsx. For women's one-day races (which are parent-level races, not stages), `selectedRace.parentRaceId` will be null, so `effectiveRaceType` will be `selectedRace.raceType` which is `womens_one_day`. The `filteredOrderTypes` filter checks `applicable.includes(effectiveRaceType)`. Since kaptein now includes `womens_one_day`, this will match. No code change needed for filtering.

The ONLY change needed: Update the `effectiveRaceType` derivation comment to note it now also handles women's one-day for kaptein. This is minimal.

Additionally, review and update the kaptein description text displayed in step 3. Currently it says "Choose your Kaptein strategy" which is generic enough. The button labels "Single Rider (x2)" and "All from Country (x1.5)" are also still accurate per ORDER-08 spec. No changes needed.

Since the UI is already dynamic and will work without code changes for the kaptein on women's races, the main verification for this task is:
1. Confirm the filtering logic works for `womens_one_day` race type
2. Confirm the kaptein UI renders correctly for this path
3. Add a brief comment near the `effectiveRaceType` calculation noting women's WC kaptein support

If truly no code change is needed (the UI dynamically adapts), still verify by tracing the data flow and document the finding.
  </action>
  <verify>
Read `src/app/(main)/leagues/[leagueId]/orders/orders-client.tsx` and trace:
1. For a `womens_one_day` race, `effectiveRaceType` = `womens_one_day`
2. `filteredOrderTypes` includes kaptein (since `applicableRaceTypes` now has `womens_one_day`)
3. Kaptein UI (`own_rider_or_country` target) renders with x2/x1.5 options
  </verify>
  <done>Kaptein order can be selected and submitted for women's one-day races via the UI, with correct x2/x1.5 strategy options displayed.</done>
</task>

</tasks>

<verification>
- `case "multiply_finish_points"` and `case "multiply_end_tour"` exist in order-queries.ts
- Old effect type names (`double_top10_stage`, `double_end_tour`) no longer referenced
- Kaptein `case "choice"` works for both WC and women's one-day races
- No TypeScript compilation errors: `npx tsc --noEmit src/lib/order-queries.ts` (or full project check)
</verification>

<success_criteria>
- Blodpose GT scoring uses correct per-GT multiplier (x3 TdF, x3.5 Giro/Vuelta)
- Etappeseier multiplies ALL own riders finish points (not just top-10) by race-specific multiplier
- Sponsorens ritt uses 3x multiplier on end-of-tour points
- Kaptein order available and scoreable for women's one-day races
- Admin can select and submit all updated orders with correct multipliers
</success_criteria>

<output>
After completion, create `.planning/phases/13-order-config-updates/13-02-SUMMARY.md`
</output>
