---
phase: 11-scoring-config-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/seed-scoring.ts
  - src/db/migrate-scoring-2026.ts
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
  - SCORE-03
  - SCORE-04
  - SCORE-05
  - SCORE-06
  - SCORE-07
  - SCORE-08
  - SCORE-09
  - SCORE-10

must_haves:
  truths:
    - "One-day race high-priority finish scoring uses new 20-position table (50 for 1st, 1 for 20th)"
    - "One-day race low-priority finish scoring uses new 15-position table (30 for 1st, 1 for 15th)"
    - "TdF stage finish scoring uses 12 positions with 15 for 1st, distinct from Giro/Vuelta 10 positions with 12 for 1st"
    - "TdF sprint scoring uses 5 positions (3/2/2/1/1), distinct from Giro (2/1/1 x2) and Vuelta (2/1/1)"
    - "TdF KOM scoring uses HCx2, HC, 1cat, 2cat, 3/4cat categories with distinct point tables"
    - "TdF end-of-tour GC uses 15 positions with 30 for 1st, Giro/Vuelta use 12 positions with 25 for 1st"
    - "GT combative jersey awards 2 points per stage"
    - "Mini tour stage finish 2nd place awards 5 points"
    - "Mini tour highest KOM uses 2/1 points"
    - "Mini tour end-of-tour GC extends to 8 positions"
  artifacts:
    - path: "src/db/seed-scoring.ts"
      provides: "Complete 2026 scoring config seed data with TdF-specific entries"
      contains: "grand_tour_tdf"
    - path: "src/db/migrate-scoring-2026.ts"
      provides: "Migration script to update existing DB scoring data to 2026 values"
      contains: "UPDATE"
  key_links:
    - from: "src/db/seed-scoring.ts"
      to: "scoringConfig table"
      via: "db.insert(scoringConfig)"
      pattern: "grand_tour_tdf.*stage_finish"
    - from: "src/db/migrate-scoring-2026.ts"
      to: "scoringConfig table"
      via: "SQL UPDATE/INSERT statements"
      pattern: "scoringConfig"
---

<objective>
Update all scoring configuration data to match the 2026 season ruleset, introducing TdF-specific scoring configs separate from Giro/Vuelta.

Purpose: The 2026 season requires distinct scoring rules for Tour de France vs Giro/Vuelta across stage finish, sprint, KOM, and end-of-tour categories. One-day races and mini tours also have updated point tables. All changes are data-only (no schema changes needed since scoringConfig.raceType is a free text field).

Output: Updated seed-scoring.ts with all 2026 values, plus a migration script to update existing DB data.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@src/db/seed-scoring.ts
@src/db/schema/config.ts
@src/lib/scoring-preview.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update seed-scoring.ts with 2026 scoring config values</name>
  <files>src/db/seed-scoring.ts</files>
  <action>
Rewrite the scoring config entries in seed-scoring.ts to match the 2026 ruleset. The key architectural change: introduce `grand_tour_tdf` as a new raceType value for TdF-specific scoring configs, while keeping `grand_tour` for Giro/Vuelta defaults.

**IMPORTANT**: The scoringConfig table uses a free `text` field for raceType (NOT the pgEnum race_type). So we can add `grand_tour_tdf` without any schema migration. The scoring lookup code (Plan 02) will map races named "Tour de France" to `grand_tour_tdf` config entries.

Specific changes to scoring entries:

**One-day races (SCORE-01):**
- `high_priority_one_day` / `finish`: Change to 20 positions: `"1": 50, "2": 40, "3": 35, "4": 30, "5": 25, "6": 20, "7": 18, "8": 16, "9": 14, "10": 12, "11": 10, "12": 9, "13": 8, "14": 7, "15": 6, "16": 5, "17": 4, "18": 3, "19": 2, "20": 1`
- `low_priority_one_day` / `finish`: Change to 15 positions: `"1": 30, "2": 25, "3": 20, "4": 16, "5": 14, "6": 12, "7": 10, "8": 8, "9": 7, "10": 6, "11": 5, "12": 4, "13": 3, "14": 2, "15": 1`

**Grand Tour stage finish (SCORE-02):**
- Keep existing `grand_tour` / `stage_finish` entry for Giro/Vuelta: 10 positions, `"1": 12, "2": 10, "3": 8, "4": 7, "5": 6, "6": 5, "7": 4, "8": 3, "9": 2, "10": 1`
- Add NEW `grand_tour_tdf` / `stage_finish`: 12 positions, `"1": 15, "2": 12, "3": 10, "4": 9, "5": 8, "6": 7, "7": 6, "8": 5, "9": 4, "10": 3, "11": 2, "12": 1`
- REMOVE the old `tdf_stage_bonus` category (replaced by the dedicated TdF stage finish entry)

**Grand Tour sprint (SCORE-03):**
- Keep existing `grand_tour` / `sprint` for Vuelta: `"1": 2, "2": 1, "3": 1` (single sprint, 3 positions)
- Add `grand_tour` / `sprint_giro`: `"1": 2, "2": 1, "3": 1` with description "Giro sprint — when 2 sprints on stage, both score with these points"
- REMOVE old `grand_tour` / `sprint_double` (replace with giro-specific handling)
- Add `grand_tour_tdf` / `sprint`: `"1": 3, "2": 2, "3": 2, "4": 1, "5": 1` (5 positions for TdF)

**Grand Tour KOM (SCORE-04):**
- Keep existing `grand_tour` mountain categories unchanged (these are Giro/Vuelta defaults)
- Add TdF-specific KOM categories:
  - `grand_tour_tdf` / `mountain_cc_hcx2_af`: `"1": 4, "2": 3, "3": 2, "4": 2, "5": 1, "6": 1`
  - `grand_tour_tdf` / `mountain_hc`: `"1": 4, "2": 3, "3": 2, "4": 1`
  - `grand_tour_tdf` / `mountain_1cat`: `"1": 3, "2": 2, "3": 1`
  - `grand_tour_tdf` / `mountain_2cat`: `"1": 2, "2": 1`
  - `grand_tour_tdf` / `mountain_3_4cat`: `"1": 1`

**Grand Tour end-of-tour (SCORE-05, SCORE-06):**
- Update `grand_tour` / `end_gc` for Giro/Vuelta: trim to 12 positions: `"1": 25, "2": 20, "3": 16, "4": 14, "5": 12, "6": 10, "7": 8, "8": 6, "9": 4, "10": 3, "11": 2, "12": 1`
- Add `grand_tour_tdf` / `end_gc`: 15 positions: `"1": 30, "2": 25, "3": 20, "4": 16, "5": 14, "6": 12, "7": 10, "8": 8, "9": 7, "10": 6, "11": 5, "12": 4, "13": 3, "14": 2, "15": 1`
- Add `grand_tour_tdf` / `end_points`: `"1": 15, "2": 10, "3": 8, "4": 6, "5": 4, "6": 2`
- Add `grand_tour_tdf` / `end_kom`: `"1": 15, "2": 10, "3": 8, "4": 6, "5": 4, "6": 2`
- Add `grand_tour_tdf` / `end_youth`: `"1": 6, "2": 4, "3": 2`
- Add `grand_tour_tdf` / `end_team`: `"1": 6, "2": 4, "3": 2`
- Add `grand_tour_tdf` / `end_combative`: `"1": 5`
- Add `grand_tour_tdf` / `end_other`: `"1": 3`
- Update `grand_tour` / `end_youth` to: `"1": 5, "2": 3, "3": 1` (Giro/Vuelta adjusted)
- Update `grand_tour` / `end_team` to: `"1": 5, "2": 3, "3": 1` (Giro/Vuelta adjusted)

**GT jersey bonuses (SCORE-07):**
- Update `grand_tour` / `jersey_combative` from `"1": 1` to `"1": 2`
- Add `grand_tour_tdf` / `jersey_gc`: `"1": 2, "2": 1, "3": 1` (same as Giro/Vuelta)
- Add `grand_tour_tdf` / `jersey_points`: `"1": 1`
- Add `grand_tour_tdf` / `jersey_kom`: `"1": 1`
- Add `grand_tour_tdf` / `jersey_combative`: `"1": 2`
- Add `grand_tour_tdf` / `ttt`: `"1": 6, "2": 4, "3": 2` (same as Giro/Vuelta)

**Mini tour updates (SCORE-08, SCORE-09, SCORE-10):**
- Update `mini_tour` / `stage_finish`: `"1": 6, "2": 5, "3": 3, "4": 2, "5": 1` (2nd place: 4 -> 5)
- Update `mini_tour` / `mountain_highest`: `"1": 2, "2": 1` (was 1/1, now 2/1)
- Update `mini_tour` / `end_gc`: extend to 8 positions: `"1": 8, "2": 6, "3": 4, "4": 3, "5": 2, "6": 2, "7": 1, "8": 1`
- Update `mini_tour` / `end_points`: `"1": 4, "2": 2, "3": 1` (unchanged — verify matches requirement)
- Update `mini_tour` / `end_kom`: `"1": 4, "2": 2, "3": 1` (unchanged — verify matches requirement)
- Update `mini_tour` / `end_youth`: `"1": 2` (unchanged)
- Update `mini_tour` / `end_combative`: `"1": 2` (unchanged)
- Update `mini_tour` / `end_team`: `"1": 2` (unchanged)

Also update `womens_one_day` / `finish` and `world_championship` / `finish` to match the new high-priority one-day values (50 for 1st, 20 positions) since they currently share the old high-priority values.

Keep the validFrom date at 2026-01-01.

Do NOT change any order type entries — those are Phase 13 scope.
Do NOT change roster limits — those are unchanged.
  </action>
  <verify>
Run `grep -c "grand_tour_tdf" src/db/seed-scoring.ts` and confirm at least 15 entries with TdF-specific raceType.
Run `grep "50.*40.*35.*30" src/db/seed-scoring.ts` to confirm updated high-priority one-day values.
Run `grep "\"2\": 5" src/db/seed-scoring.ts` near mini_tour stage_finish to confirm 2nd place updated.
Verify no TypeScript syntax errors: `npx tsc --noEmit src/db/seed-scoring.ts` or equivalent check.
  </verify>
  <done>
seed-scoring.ts contains complete 2026 scoring config with: (1) updated one-day point tables, (2) grand_tour_tdf entries for all TdF-specific categories, (3) updated Giro/Vuelta defaults in grand_tour entries, (4) updated mini tour values, (5) combative jersey at 2 points. No schema changes required.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create migration script for existing DB scoring data</name>
  <files>src/db/migrate-scoring-2026.ts</files>
  <action>
Create a migration script `src/db/migrate-scoring-2026.ts` that updates the existing scoringConfig rows in the production database to match the 2026 ruleset. This script is needed because seed-scoring.ts only runs on empty databases.

The script should:
1. Import db from `@/lib/db` and scoringConfig from `@/db/schema/config`
2. Use a transaction wrapping all changes
3. For each existing scoring config row that needs updating, use `db.update(scoringConfig).set({ rules: newRules }).where(and(eq(scoringConfig.raceType, X), eq(scoringConfig.category, Y)))` pattern
4. For new entries (all `grand_tour_tdf` rows, updated sprint categories), use `db.insert(scoringConfig).values(...)` with onConflict ignore (in case script runs twice)
5. Delete the obsolete `tdf_stage_bonus` and `sprint_double` entries

Specific operations:
- UPDATE `high_priority_one_day` / `finish` rules to new 20-position table
- UPDATE `low_priority_one_day` / `finish` rules to new 15-position table
- UPDATE `grand_tour` / `end_gc` rules (trim to 12 positions)
- UPDATE `grand_tour` / `end_youth` to `"1": 5, "2": 3, "3": 1`
- UPDATE `grand_tour` / `end_team` to `"1": 5, "2": 3, "3": 1`
- UPDATE `grand_tour` / `jersey_combative` to `"1": 2`
- UPDATE `mini_tour` / `stage_finish` to `"1": 6, "2": 5, "3": 3, "4": 2, "5": 1`
- UPDATE `mini_tour` / `mountain_highest` to `"1": 2, "2": 1`
- UPDATE `mini_tour` / `end_gc` to 8 positions
- UPDATE `womens_one_day` / `finish` and `world_championship` / `finish` to new 20-position table
- DELETE `grand_tour` / `tdf_stage_bonus`
- DELETE `grand_tour` / `sprint_double`
- INSERT all new `grand_tour_tdf` entries (stage_finish, sprint, mountain categories, jersey categories, ttt, end-of-tour categories)
- INSERT `grand_tour` / `sprint_giro` (Giro-specific sprint)

Add console logging for each operation. Include a dry-run mode via `--dry-run` CLI argument that logs what would change without committing.

The script should be runnable via `npx tsx src/db/migrate-scoring-2026.ts`.
  </action>
  <verify>
Run `npx tsx src/db/migrate-scoring-2026.ts --dry-run` to confirm it parses and lists all planned changes without error.
Verify the script imports compile correctly.
Count the operations: should be ~10 UPDATEs, ~2 DELETEs, ~15+ INSERTs.
  </verify>
  <done>
Migration script exists, compiles, runs in dry-run mode listing all changes, and when run for real updates existing scoring data to 2026 values and inserts all TdF-specific entries.
  </done>
</task>

</tasks>

<verification>
- seed-scoring.ts contains all 2026 scoring values matching REQUIREMENTS.md SCORE-01 through SCORE-10
- grand_tour_tdf raceType entries exist for stage_finish, sprint, all mountain categories, all jersey categories, ttt, all end-of-tour categories
- Giro/Vuelta entries remain under grand_tour raceType with updated values
- Migration script handles both fresh DBs (via seed) and existing DBs (via migration)
- No schema changes needed (scoringConfig.raceType is text, not enum)
</verification>

<success_criteria>
- All 10 SCORE requirement point values are present in seed-scoring.ts
- TdF-specific entries use `grand_tour_tdf` raceType consistently
- Migration script runs without error in dry-run mode
- No TypeScript compilation errors in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/11-scoring-config-update/11-01-SUMMARY.md`
</output>
