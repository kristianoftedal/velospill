---
phase: 27-league-stage-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - src/app/(main)/leagues/[leagueId]/standings/standings-client.tsx
  - src/app/(main)/leagues/[leagueId]/standings/page.tsx
autonomous: false
requirements:
  - SVIS-01
  - SVIS-02
  - SVIS-03

must_haves:
  truths:
    - "Multi-stage race rows in the Race Results tab have a chevron expand control"
    - "Clicking expand reveals individual stage rows with stage name, league points, Done/Pending badge, and link to breakdown page"
    - "Pending stages (hasResults=false) show a Pending badge and have no clickable link"
    - "Done stages (hasResults=true) show the points and a link to /leagues/[leagueId]/standings/[stageRaceId]"
    - "End-of-Tour section appears below stage list only when endOfTourPoints > 0"
    - "All multi-stage race rows start collapsed on page load"
    - "One-day races render as before — flat non-expandable rows"
    - "Collapsing the row returns to the compact view"
  artifacts:
    - path: "src/app/(main)/leagues/[leagueId]/standings/standings-client.tsx"
      provides: "Expandable race rows in Race Results tab"
    - path: "src/app/(main)/leagues/[leagueId]/standings/page.tsx"
      provides: "Passes LeagueRaceScoreGrouped[] to StandingsClient"
  key_links:
    - from: "standings/page.tsx"
      to: "StandingsClient"
      via: "races prop typed as LeagueRaceScoreGrouped[]"
      pattern: "LeagueRaceScoreGrouped"
    - from: "StandingsClient expand control"
      to: "stage row list"
      via: "useState expandedRaces Set<number>"
      pattern: "expandedRaces\\.has"
---

<objective>
Update `StandingsClient` to render expandable multi-stage race rows in the Race Results tab. Each expanded row shows per-stage breakdown with Done/Pending badges and breakdown links, plus an End-of-Tour section when end-of-tour results exist. Update `standings/page.tsx` prop type to match.

Purpose: Delivers SVIS-01 (expandable rows), SVIS-02 (per-stage points), SVIS-03 (end-of-tour classifications).
Output: Interactive Race Results tab with accordion-style grand tour expansion.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-league-stage-visibility/27-CONTEXT.md
@.planning/phases/27-league-stage-visibility/27-01-SUMMARY.md

<interfaces>
<!-- Types produced by plan 01. Use these directly. -->

From src/lib/scoring-queries.ts (after plan 01):
```typescript
export type StageScore = {
  raceId: number
  raceName: string
  stageNumber: number | null
  startDate: Date
  totalLeaguePoints: number
  hasResults: boolean
}

export type LeagueRaceScoreGrouped = {
  raceId: number
  raceName: string
  raceType: string
  startDate: Date
  totalLeaguePoints: number
  isMultiStage: boolean
  stages: StageScore[]
  endOfTourPoints: number
}
```

Current StandingsClient prop interface:
```typescript
interface StandingsClientProps {
  standings: LeagueStanding[]
  myTeamRiders: TeamRiderScore[] | null
  leagueId: number
  userTeamName: string | null
  userTeamId: number | null
  races: LeagueRaceScore[]  // <-- change to LeagueRaceScoreGrouped[]
}
```

Available shadcn/ui components already imported or easily imported:
- Table, TableBody, TableCell, TableHead, TableHeader, TableRow
- Badge (variant="secondary" for Done, variant="outline" for Pending)
- ChevronRight, ChevronDown from lucide-react
- Link from next/link
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update standings-client.tsx with expandable multi-stage rows</name>
  <files>src/app/(main)/leagues/[leagueId]/standings/standings-client.tsx</files>
  <action>
**Import changes:**
- Replace `LeagueRaceScore` import with `LeagueRaceScoreGrouped, StageScore` from `@/lib/scoring-queries`
- Add `{ useState }` to React import (add `"use client"` is already present)
- Add `ChevronRight, ChevronDown` from `lucide-react`

**Update prop type:**
```typescript
races: LeagueRaceScoreGrouped[]
```

**Add expand state inside the component:**
```typescript
const [expandedRaces, setExpandedRaces] = useState<Set<number>>(new Set())

function toggleRace(raceId: number) {
  setExpandedRaces(prev => {
    const next = new Set(prev)
    if (next.has(raceId)) next.delete(raceId)
    else next.add(raceId)
    return next
  })
}
```

**Race Results TabsContent — replace the map with conditional rendering:**

For each race in `races`:

If `race.isMultiStage`:
- Render an expandable parent row with a chevron toggle button:
  - Left: chevron icon (ChevronDown when expanded, ChevronRight when collapsed) + race name (plain text, not a link — the parent is not a breakdown page target)
  - Middle: Badge with formatted raceType
  - Right date column: format startDate
  - Rightmost: total league points
  - The entire row or the chevron cell is clickable to toggle expand
- When expanded, render a sub-section inside a TableRow with colSpan={4}:
  - For each stage in `race.stages` (already sorted by stageNumber/startDate from plan 01):
    - Indent visually (pl-8 or similar)
    - Stage name (link to `/leagues/[leagueId]/standings/[stage.raceId]` if `stage.hasResults`, else plain text in muted color)
    - Badge: `variant="secondary"` with "Done" if `stage.hasResults`, `variant="outline"` with "Pending" if not (consistent with Phase 26 admin Done/Pending pattern)
    - Points: `stage.totalLeaguePoints` (show 0 if pending, still render the number)
  - If `race.endOfTourPoints > 0`, render a labeled section below stages:
    - A small header row: "Final Classifications" (muted, text-xs uppercase tracking-wide)
    - A single row: race name + link to `/leagues/[leagueId]/standings/[race.raceId]` + points `race.endOfTourPoints`

If `!race.isMultiStage` (one-day races):
- Render the existing flat TableRow unchanged (name as link, badge, date, points)

**Styling guidance (Claude's discretion per CONTEXT.md):**
- Parent row: `cursor-pointer hover:bg-gray-50` on the row, chevron in first cell
- Expanded sub-rows: `bg-gray-50` background, `pl-8` indent on name cell
- Pending stage name: `text-gray-400` muted, no hover underline
- Section header "Final Classifications": `text-xs font-semibold uppercase tracking-wide text-gray-500 pt-2 pb-1`

The expand sub-section should be rendered as a single `<TableRow>` with a `<TableCell colSpan={4}>` containing a nested `<div>` with `<table>` or simple `<div>` rows — do NOT try to nest `<table>` inside `<TableBody>` as that is invalid HTML. Use a plain `<div>` grid or `<table>` inside the `<TableCell>`.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/velospill && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles. StandingsClient accepts `LeagueRaceScoreGrouped[]`. Component has expand state. Multi-stage rows show chevron. One-day rows unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update standings/page.tsx prop type</name>
  <files>src/app/(main)/leagues/[leagueId]/standings/page.tsx</files>
  <action>
The `races` variable from `getLeagueRacesWithScores` now returns `LeagueRaceScoreGrouped[]` (changed in plan 01). The page passes this directly to `StandingsClient`. No logic change is needed — only the import and TypeScript type must align.

Update the import in `standings/page.tsx`:
- Remove `LeagueRaceScore` from the import if it was imported (it likely was not imported explicitly — the type is inferred)
- Verify the call `getLeagueRacesWithScores(leagueId, seasonYear)` compiles with the new return type
- If `LeagueRaceScore` appears anywhere in the page file, replace with `LeagueRaceScoreGrouped`

This task should be minimal — typically 0-3 lines changed. If TypeScript already infers correctly from plan 01's return type change, no changes may be needed in the page file at all.
  </action>
  <verify>
    <automated>cd /Users/kristianoftedal/dev/velospill && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles without errors across the standings page and client.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify expandable race rows in browser</name>
  <what-built>
    Expandable multi-stage race rows in the Race Results tab on league standings page.
    - Multi-stage races: chevron toggle, expand reveals stage list with Done/Pending badges and breakdown links
    - End-of-tour section appears only when endOfTourPoints > 0
    - One-day races unchanged
    - All grand tour rows start collapsed
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Navigate to a league's standings page: `/leagues/[leagueId]/standings`
    3. Click "Race Results" tab
    4. Verify: multi-stage races (grand tours, mini tours) show a chevron icon on the left side of the row
    5. Click a grand tour row — it should expand to reveal individual stage rows
    6. Stages with results: "Done" badge, clickable name link to the breakdown page, points shown
    7. Stages without results: "Pending" badge, muted name (no link), 0 points
    8. If any grand tour has end-of-tour classification results: "Final Classifications" section appears below stages with points and link
    9. Click the row again — it collapses back to the parent row only
    10. One-day races: unchanged flat rows with links, no chevron
    11. Verify collapse/expand works for multiple grand tours independently
  </how-to-verify>
  <resume-signal>Type "approved" if the UI works correctly, or describe any issues found</resume-signal>
  <files>src/app/(main)/leagues/[leagueId]/standings/standings-client.tsx</files>
  <action>Human verifies the expandable race rows work correctly in the browser per the steps above.</action>
  <verify>
    <automated>MISSING — human-only verification step</automated>
  </verify>
  <done>User has confirmed expandable multi-stage rows render and function correctly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Race Results tab renders multi-stage rows with chevron expand controls
- Expanding reveals stage list with Done/Pending badges
- End-of-tour section shows only when endOfTourPoints > 0
- One-day races unchanged
- Collapsing returns to compact view
</verification>

<success_criteria>
- SVIS-01: Multi-stage race rows expandable with chevron control
- SVIS-02: Each stage shows name, league points, Done/Pending status, and link to breakdown
- SVIS-03: End-of-tour section visible within expanded grand tour view when results exist
- TypeScript compiles without errors
- One-day race rows visually unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/27-league-stage-visibility/27-02-SUMMARY.md`
</output>
