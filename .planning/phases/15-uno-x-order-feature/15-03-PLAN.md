---
phase: 15-uno-x-order-feature
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - src/app/admin/orders/actions.ts
  - src/app/admin/orders/page.tsx
  - src/app/admin/orders/bonus-rider-draft.tsx
  - src/app/(main)/leagues/[leagueId]/orders/bonus-rider-pick.tsx
  - src/app/(main)/leagues/[leagueId]/orders/page.tsx
  - src/app/(main)/leagues/[leagueId]/orders/orders-client.tsx
autonomous: true
requirements:
  - ORDER-07

must_haves:
  truths:
    - "Admin can activate Uno-X orders and see the reverse standings draft order"
    - "Admin can see which bonus riders were picked per team per GT"
    - "Each team picks one bonus rider from the unowned pool in draft order"
    - "Draft order follows reverse standings (last place picks first)"
    - "Picked bonus riders are visible in the team's orders section"
  artifacts:
    - path: "src/app/admin/orders/bonus-rider-draft.tsx"
      provides: "Admin UI showing draft order, pick status, and bonus rider selections per GT"
      contains: "BonusRiderDraft"
    - path: "src/app/(main)/leagues/[leagueId]/orders/bonus-rider-pick.tsx"
      provides: "Team UI for picking a bonus rider from the unowned pool"
      contains: "BonusRiderPick"
    - path: "src/app/admin/orders/actions.ts"
      provides: "Admin action for activating Uno-X orders with bonus rider draft"
      contains: "uno_x|bonus_rider"
  key_links:
    - from: "src/app/admin/orders/bonus-rider-draft.tsx"
      to: "src/lib/order-queries.ts"
      via: "getBonusRidersForRace, computeReverseDraftOrder"
      pattern: "getBonusRidersForRace|computeReverseDraftOrder"
    - from: "src/app/(main)/leagues/[leagueId]/orders/bonus-rider-pick.tsx"
      to: "src/lib/order-queries.ts"
      via: "getUnownedRidersForGT, saveBonusRiderPick"
      pattern: "saveBonusRiderPick"
    - from: "src/app/admin/orders/page.tsx"
      to: "src/app/admin/orders/bonus-rider-draft.tsx"
      via: "component import and rendering"
      pattern: "BonusRiderDraft"
---

<objective>
Build the admin and team UI for the Uno-X bonus rider draft: admin can see and manage the draft process, and each team can pick their bonus rider in reverse standings order.

Purpose: Completes the Uno-X feature by providing the interactive UI for the bonus rider draft. Admin activates the order, sees the draft order and picks. Teams see when it's their turn and can select from unowned riders.

Output: Admin bonus rider draft management component, team bonus rider picking component, and integration into existing pages.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-uno-x-order-feature/15-01-SUMMARY.md
@.planning/phases/15-uno-x-order-feature/15-02-SUMMARY.md

@src/app/admin/orders/page.tsx
@src/app/admin/orders/actions.ts
@src/app/(main)/leagues/[leagueId]/orders/orders-client.tsx
@src/app/(main)/leagues/[leagueId]/orders/page.tsx
@src/lib/order-queries.ts
@src/db/schema/bonus-riders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build admin bonus rider draft management UI</name>
  <files>
    src/app/admin/orders/bonus-rider-draft.tsx
    src/app/admin/orders/page.tsx
    src/app/admin/orders/actions.ts
  </files>
  <action>
    **In `src/app/admin/orders/actions.ts`:**

    1. Add `getActivatedUnoXOrders()` function:
       - Fetches all Uno-X orders with status "active", joined with leagues, races, teams
       - Groups by league+race to show one section per GT per league
       - Returns: `{ leagueId, leagueName, raceId, raceName, season, orders: { orderId, teamId, teamName }[] }[]`

    2. Add `getBonusRiderDraftState(leagueId, raceId, season)` function:
       - Calls `computeReverseDraftOrder(leagueId, season)` from order-queries.ts to get draft order
       - Calls `getBonusRidersForRace(leagueId, raceId)` to get already-made picks
       - Returns combined state: `{ draftOrder: { teamId, teamName, totalPoints, pickOrder }[], picks: { teamId, teamName, riderId, riderName, riderTeam, pickOrder }[], allPicked: boolean }`

    **In `src/app/admin/orders/bonus-rider-draft.tsx`:**

    3. Create `BonusRiderDraft` client component:
       - Props: `activatedDrafts` (from getActivatedUnoXOrders), `getDraftState` server action function
       - Shows a Card for each active Uno-X draft (league + GT combination)
       - Each card shows:
         a. GT race name and league name as header
         b. Draft order table: columns = Pick #, Team, Points, Status, Bonus Rider
         c. Status column shows "Picked" (green badge) with rider name or "Waiting" (gray badge)
         d. If all teams have picked: show "Draft Complete" indicator
       - Uses `useEffect` to load draft state for each draft via server action on mount
       - Follows existing admin patterns: Card/Table components from shadcn, same styling as order history table

    **In `src/app/admin/orders/page.tsx`:**

    4. Import and render `BonusRiderDraft` component between the Pending Orders and Order History sections:
       - Fetch activated Uno-X drafts via `getActivatedUnoXOrders()`
       - Pass data and `getBonusRiderDraftState` server action as props
       - Only render the section if there are active Uno-X orders
  </action>
  <verify>
    - Admin orders page shows Bonus Rider Draft section when Uno-X orders are active
    - Draft order is displayed in reverse standings (lowest points first)
    - Picks made by teams are visible with rider name and team
    - "Draft Complete" shows when all teams have picked
    - TypeScript compiles without errors
  </verify>
  <done>
    Admin can see all active Uno-X bonus rider drafts, the reverse standings draft order for each, and which teams have picked their bonus rider
  </done>
</task>

<task type="auto">
  <name>Task 2: Build team bonus rider picking UI</name>
  <files>
    src/app/(main)/leagues/[leagueId]/orders/bonus-rider-pick.tsx
    src/app/(main)/leagues/[leagueId]/orders/page.tsx
    src/app/(main)/leagues/[leagueId]/orders/orders-client.tsx
  </files>
  <action>
    **Create `src/app/(main)/leagues/[leagueId]/orders/bonus-rider-pick.tsx`:**

    1. Create `BonusRiderPick` client component:
       - Props:
         - `leagueId: number`
         - `teamId: number`
         - `raceId: number`
         - `raceName: string`
         - `draftOrder: { teamId: number; teamName: string; pickOrder: number; totalPoints: number }[]`
         - `existingPicks: { teamId: number; riderName: string; riderTeam: string }[]`
         - `availableRiders: { riderId: number; riderName: string; riderTeam: string; nationality: string }[]`
         - `myPickOrder: number`
         - `isMyTurn: boolean`
         - `alreadyPicked: boolean`
         - `pickBonusRider: (leagueId: number, teamId: number, riderId: number, raceId: number) => Promise<{ success: true } | { success: false; error: string }>`
       - Shows a Card with:
         a. Header: "Bonus Rider Draft - {raceName}"
         b. Draft order mini-table: Pick #, Team, Status (Picked/Your Turn/Waiting)
         c. If `alreadyPicked`: show which rider was picked with green checkmark
         d. If `isMyTurn`: show rider selection grid (same pattern as own_rider target in orders-client.tsx — grid of rider cards with name and team, scrollable, max-h-64)
         e. If not `isMyTurn` and not `alreadyPicked`: show waiting message with "It's not your turn yet. Teams ahead of you still need to pick."
       - Uses purple accent color consistent with orders UI
       - On rider selection: calls `pickBonusRider` server action, shows toast on success/failure
       - Search input to filter available riders by name (client-side filter)

    **Add server action in `src/app/(main)/leagues/[leagueId]/orders/actions.ts`:**

    2. Add `pickBonusRider(leagueId, teamId, riderId, raceId)` server action:
       - Auth check (getAuthenticatedUser)
       - League membership check (checkLeagueMembership)
       - Verify the team has an active Uno-X order for this race
       - Verify the rider is unowned (not in draftPicks for this league)
       - Verify the rider is not already picked as a bonus rider for this league+race
       - Verify it's the team's turn (compute draft order, check picks count matches pickOrder - 1)
       - Call `saveBonusRiderPick` from order-queries.ts
       - Revalidate paths: `/leagues/{leagueId}/orders`, `/admin/orders`
       - Return `{ success: true }` or `{ success: false; error: string }`

    **Update `src/app/(main)/leagues/[leagueId]/orders/page.tsx`:**

    3. After the existing OrdersClient, add a section for active bonus rider drafts:
       - Query for active Uno-X orders for this team
       - For each active Uno-X order, compute:
         - Draft order via `computeReverseDraftOrder`
         - Existing picks via `getBonusRidersForRace`
         - Available riders via `getUnownedRidersForGT`
         - Whether it's this team's turn
         - Whether this team already picked
       - Render `BonusRiderPick` component for each active Uno-X draft
       - Pass `pickBonusRider` server action as prop

    **Update `src/app/(main)/leagues/[leagueId]/orders/orders-client.tsx`:**

    4. In the My Orders table, add special display for Uno-X orders:
       - If order status is "active" and orderTypeName includes "uno_x", show "Bonus Draft Active" link/badge
       - This is just a visual indicator; the actual draft UI is rendered separately via BonusRiderPick
  </action>
  <verify>
    - Team can see the bonus rider draft section when they have an active Uno-X order
    - Draft order shows all teams in reverse standings order
    - Team can pick a rider when it's their turn
    - Team sees their picked rider after selection
    - Team sees "waiting" message when it's not their turn
    - Server action validates turn order, rider ownership, and uniqueness
    - TypeScript compiles without errors
  </verify>
  <done>
    Teams can pick bonus riders through the orders page in reverse standings order; picks are validated server-side for turn order, rider ownership, and uniqueness; admin and team UIs both reflect the current draft state
  </done>
</task>

</tasks>

<verification>
1. Admin orders page shows Bonus Rider Draft section with draft order and pick status
2. Team orders page shows bonus rider draft when they have an active Uno-X order
3. Turn-based picking enforced (last place picks first)
4. Pick validation prevents: out-of-turn picks, owned riders, duplicate picks
5. Picks immediately visible in both admin and team views after revalidation
6. All TypeScript compiles without errors
</verification>

<success_criteria>
- Admin can see all active Uno-X drafts with reverse standings order and pick status
- Each team can pick a bonus rider when it's their turn
- Draft order is enforced server-side (last place picks first)
- Picked bonus riders are visible to both admin and team
- The complete flow works: submit order -> admin activates -> teams pick in order -> bonus rider scores for GT
</success_criteria>

<output>
After completion, create `.planning/phases/15-uno-x-order-feature/15-03-SUMMARY.md`
</output>
