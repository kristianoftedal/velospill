---
phase: 13-order-config-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migrate-order-types-v1.1.ts
  - src/db/seed-scoring.ts
autonomous: true
requirements:
  - ORDER-01
  - ORDER-02
  - ORDER-03
  - ORDER-04
  - ORDER-05
  - ORDER-08

must_haves:
  truths:
    - "Blodpose GT uses x3 for TdF and x3.5 for Giro/Vuelta in DB"
    - "Etappeseier uses multiply_finish_points effect type with per-GT multipliers in DB"
    - "Hammer uses 5 points per position and max 50 in DB"
    - "Lagtempo uses 10 points per top-20 placement in DB"
    - "Sponsorens ritt uses multiplier value 3 in DB"
    - "Kaptein applicableRaceTypes includes womens_one_day in DB"
  artifacts:
    - path: "scripts/migrate-order-types-v1.1.ts"
      provides: "Migration script for updating orderTypes JSONB data"
      min_lines: 60
    - path: "src/db/seed-scoring.ts"
      provides: "Updated seed data matching new order type configurations"
      contains: "multiply_finish_points"
  key_links:
    - from: "scripts/migrate-order-types-v1.1.ts"
      to: "orderTypes table"
      via: "SQL UPDATE statements"
      pattern: "UPDATE.*orderTypes.*SET.*effect"
---

<objective>
Update all order type seed data and create migration script to match 2026 ruleset for order multipliers and configurations.

Purpose: Phase 13 requires updated order multipliers (Blodpose GT split, Etappeseier restructure, Hammer/Lagtempo value bumps, Sponsorens ritt 3x, Kaptein for women's WC). The DB orderTypes JSONB effect fields must be updated before the application logic can use the new values.

Output: Migration script for production data + updated seed file for fresh installs.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/seed-scoring.ts
@src/db/schema/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration script for orderTypes updates</name>
  <files>scripts/migrate-order-types-v1.1.ts</files>
  <action>
Create a migration script at `scripts/migrate-order-types-v1.1.ts` following the pattern from phase 11 migration scripts (Pool.connect() for DDL, transaction wrapping, dry-run flag, console logging per operation).

The script must update the following orderTypes records by `name`:

1. **blodpose_gt** (ORDER-01): Update `effect` JSONB from `{"type":"multiplier","value":3,"target":"own_rider"}` to `{"type":"multiplier","values":{"grand_tour":3.5,"grand_tour_tdf":3},"target":"own_rider"}`. Also update `description` to "x3 for TdF or x3.5 for Giro/Vuelta for one of your riders".

2. **etappeseier** (ORDER-02): Update `effect` JSONB from `{"type":"double_top10_stage","target":"all_own_riders"}` to `{"type":"multiply_finish_points","values":{"grand_tour":2.25,"grand_tour_tdf":2},"target":"all_own_riders"}`. Also update `description` to "All your riders get multiplied stage finish points (x2 TdF, x2.25 Giro/Vuelta)".

3. **hammer** (ORDER-03): Update `effect` JSONB field `points_per_position` from 3 to 5 and `max_points` from 30 to 50. Also update `description` to "Pick a GC top-10 rider not on your team, 5 pts per GC position lost, max 50 (stage 11+)".

4. **lagtempo** (ORDER-04): Update `effect` JSONB field `points_per_top20` from 5 to 10. Also update `description` to "Choose a real team, 10 pts per top-20 placement (Vuelta only, can't play on TTT stage)".

5. **sponsorens_ritt** (ORDER-05): Update `effect` JSONB from `{"type":"double_end_tour","target":"all_own_riders"}` to `{"type":"multiply_end_tour","value":3,"target":"all_own_riders"}`. Also update `description` to "3x end-of-tour points for all your riders".

6. **kaptein** (ORDER-08): Update `applicableRaceTypes` JSONB from `["world_championship"]` to `["world_championship","womens_one_day"]`. Also update `description` to "x2 for one rider OR x1.5 for all riders from a chosen country (WC and Women's races)".

Use SQL UPDATE with WHERE name = 'xxx' for each. Wrap all 6 updates in a single transaction. Support `--dry-run` flag that logs SQL but does not commit. Use the `pg` Pool import pattern: `import pg from "pg"` then `const pool = new pg.Pool({ connectionString: process.env.DATABASE_URL })`.

Log each update with the order name and old/new values for audit trail.
  </action>
  <verify>Run `npx tsx scripts/migrate-order-types-v1.1.ts --dry-run` and confirm it logs all 6 planned UPDATE statements without errors.</verify>
  <done>Migration script exists, runs in dry-run mode without errors, and covers all 6 orderType updates.</done>
</task>

<task type="auto">
  <name>Task 2: Update seed-scoring.ts with new order type configurations</name>
  <files>src/db/seed-scoring.ts</files>
  <action>
Update the orderEntries array in `src/db/seed-scoring.ts` to match the new 2026 order configurations. These changes ensure fresh installs get the correct data:

1. **blodpose_gt**: Change `effect` from `{ type: "multiplier", value: 3, target: "own_rider" }` to `{ type: "multiplier", values: { grand_tour: 3.5, grand_tour_tdf: 3 }, target: "own_rider" }`. Update description to "x3 for TdF or x3.5 for Giro/Vuelta for one of your riders".

2. **etappeseier**: Change `effect` from `{ type: "double_top10_stage", target: "all_own_riders" }` to `{ type: "multiply_finish_points", values: { grand_tour: 2.25, grand_tour_tdf: 2 }, target: "all_own_riders" }`. Update description to "All your riders get multiplied stage finish points (x2 TdF, x2.25 Giro/Vuelta)".

3. **hammer**: Change effect `points_per_position` from 3 to 5 and `max_points` from 30 to 50. Update description to "Pick a GC top-10 rider not on your team, 5 pts per GC position lost, max 50 (stage 11+)".

4. **lagtempo**: Change effect `points_per_top20` from 5 to 10. Update description to "Choose a real team, 10 pts per top-20 placement (Vuelta only, can't play on TTT stage)".

5. **sponsorens_ritt**: Change `effect` from `{ type: "double_end_tour", target: "all_own_riders" }` to `{ type: "multiply_end_tour", value: 3, target: "all_own_riders" }`. Update description to "3x end-of-tour points for all your riders".

6. **kaptein**: Change `applicableRaceTypes` from `["world_championship"]` to `["world_championship", "womens_one_day"]`. Update description to "x2 for one rider OR x1.5 for all riders from a chosen country (WC and Women's races)".

Do NOT change any other entries. Only modify the 6 order entries listed above.
  </action>
  <verify>Read src/db/seed-scoring.ts and confirm: blodpose_gt has `values` key (not `value`), etappeseier has `multiply_finish_points` type, hammer has `points_per_position: 5`, lagtempo has `points_per_top20: 10`, sponsorens_ritt has `multiply_end_tour` type with `value: 3`, kaptein has `womens_one_day` in applicableRaceTypes.</verify>
  <done>seed-scoring.ts reflects all 6 order type updates for 2026 season, matching the migration script exactly.</done>
</task>

</tasks>

<verification>
- Migration script runs in dry-run mode: `npx tsx scripts/migrate-order-types-v1.1.ts --dry-run`
- Seed data file has no syntax errors: `npx tsx --eval "import('./src/db/seed-scoring.ts')"`
- All 6 order types updated in both files with matching values
</verification>

<success_criteria>
- Migration script covers ORDER-01, ORDER-02, ORDER-03, ORDER-04, ORDER-05, ORDER-08 DB changes
- Seed file matches migration target values for fresh installs
- Both files use consistent effect JSONB structures
</success_criteria>

<output>
After completion, create `.planning/phases/13-order-config-updates/13-01-SUMMARY.md`
</output>
