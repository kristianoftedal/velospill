---
phase: 02-admin-backoffice-race-calendar
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/admin/transfers/page.tsx
  - src/app/admin/orders/page.tsx
  - src/app/admin/page.tsx
  - src/app/admin/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can navigate to a transfers management page"
    - "Admin can navigate to an orders validation page"
    - "Transfer page shows shell UI with placeholder for future transfer bid list"
    - "Orders page shows shell UI with placeholder for future order validation"
    - "Admin dashboard shows cards for Results, Transfers, and Orders alongside existing Riders and Races"
  artifacts:
    - path: "src/app/admin/transfers/page.tsx"
      provides: "Transfer management shell page with placeholder UI"
      contains: "Transfer"
    - path: "src/app/admin/orders/page.tsx"
      provides: "Order validation shell page with placeholder UI"
      contains: "Order"
    - path: "src/app/admin/page.tsx"
      provides: "Updated admin dashboard with cards for all admin sections"
      contains: "Results"
  key_links:
    - from: "src/app/admin/layout.tsx"
      to: "src/app/admin/transfers/page.tsx"
      via: "nav link to /admin/transfers"
      pattern: "transfers"
    - from: "src/app/admin/layout.tsx"
      to: "src/app/admin/orders/page.tsx"
      via: "nav link to /admin/orders"
      pattern: "orders"
---

<objective>
Create admin shell pages for transfer management and order validation, plus update the admin dashboard (ADMIN-06, ADMIN-07, ADMIN-08 shells).

Purpose: These are placeholder pages that establish the admin UI structure for features built in Phases 5 (transfers) and 7 (orders). Building the shell now keeps the admin backoffice cohesive and lets the user see the full admin navigation.
Output: Shell pages at /admin/transfers and /admin/orders, updated admin dashboard with all section cards.
</objective>

<execution_context>
@/Users/kristianoftedal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kristianoftedal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/admin/page.tsx
@src/app/admin/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create transfer and order shell pages</name>
  <files>src/app/admin/transfers/page.tsx, src/app/admin/orders/page.tsx</files>
  <action>
**src/app/admin/transfers/page.tsx** (server component):
- Import Card, CardContent, CardDescription, CardHeader, CardTitle from shadcn
- Import Badge from shadcn
- Page title: "Transfer Management"
- Description: "Review and approve/reject transfer bids during transfer windows"
- Show two sections as Cards:

1. "Transfer Window Status" card:
   - Badge showing "No Active Window" (muted variant)
   - Text: "Transfer windows will be configured when the transfer system is built in a later phase."
   - Empty state with calendar icon placeholder

2. "Pending Transfer Bids" card:
   - Empty state message: "No transfer bids to review"
   - Text: "Transfer bids from league members will appear here during active transfer windows."
   - Placeholder for future DataTable of bids (just the empty state for now)

Style consistently with existing admin pages (same Card layout, spacing, typography).

**src/app/admin/orders/page.tsx** (server component):
- Import Card, CardContent, CardDescription, CardHeader, CardTitle from shadcn
- Import Badge from shadcn
- Page title: "Order Validation"
- Description: "Validate submitted orders and verify their effects are correctly applied"
- Show two sections as Cards:

1. "Pending Orders" card:
   - Empty state message: "No orders to validate"
   - Text: "Submitted orders from league members will appear here for validation."

2. "Order History" card:
   - Empty state message: "No orders processed yet"
   - Text: "Validated and applied orders will appear here with their effects."

Style consistently with existing admin pages.

Both pages should be clearly marked as placeholder/shell that will be populated in later phases. Do NOT import or reference any tables that don't exist yet (no transfer tables, no order tables). These are pure UI shells.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify pages compile. Run `npm run build` to verify pages render.
  </verify>
  <done>Transfer and order shell pages exist at /admin/transfers and /admin/orders with informative placeholder UI.</done>
</task>

<task type="auto">
  <name>Task 2: Update admin dashboard and navigation with new sections</name>
  <files>src/app/admin/page.tsx, src/app/admin/layout.tsx</files>
  <action>
**Update src/app/admin/layout.tsx:**
- Add nav links for "Results", "Transfers", and "Orders" alongside existing "Riders" and "Races"
- Note: Plan 02-02 also adds "Results" to nav - if that runs first, don't duplicate. If this runs first, add all three. The link format should match existing pattern:
```tsx
<Link href="/admin/results" className="text-sm text-muted-foreground hover:text-foreground transition-colors">
  Results
</Link>
<Link href="/admin/transfers" className="text-sm text-muted-foreground hover:text-foreground transition-colors">
  Transfers
</Link>
<Link href="/admin/orders" className="text-sm text-muted-foreground hover:text-foreground transition-colors">
  Orders
</Link>
```

**Update src/app/admin/page.tsx:**
- Keep existing Riders and Races cards
- Add new cards in the grid (maintain lg:grid-cols-3 or expand to accommodate):

1. "Results" card:
   - Description: "Race results and scoring"
   - Show count of races with results entered (query raceResults for distinct raceIds count) - import raceResults from schema
   - Button: "Manage Results" linking to /admin/results

2. "Transfers" card:
   - Description: "Transfer bid management"
   - Show "Coming soon" or "0 pending" as placeholder
   - Button: "View Transfers" linking to /admin/transfers

3. "Orders" card:
   - Description: "Order validation"
   - Show "Coming soon" or "0 pending" as placeholder
   - Button: "View Orders" linking to /admin/orders

For the Results count, add: `import { raceResults } from "@/db/schema/results"` and query `db.select({ count: countDistinct(raceResults.raceId) }).from(raceResults)`. Use `countDistinct` from `drizzle-orm`.

For Transfers and Orders counts, just show static "0" since the tables don't exist yet. Add a comment noting these will be dynamic once the respective phases are built.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify dashboard compiles. Navigate to /admin and verify all 5 cards display. Verify all nav links work. Run `npm run build` for full build check.
  </verify>
  <done>Admin dashboard shows 5 section cards (Riders, Races, Results, Transfers, Orders). Admin nav has links to all 5 sections. Transfer and order cards show placeholder counts.</done>
</task>

</tasks>

<verification>
- /admin dashboard shows 5 cards with working links
- /admin/transfers shows transfer management shell
- /admin/orders shows order validation shell
- Admin nav bar has Riders, Races, Results, Transfers, Orders links
- All links navigate correctly
- Shell pages clearly communicate "coming in a later phase" status
</verification>

<success_criteria>
- ADMIN-06 shell: Transfer management page exists with placeholder UI for bid review
- ADMIN-07 shell: Transfer window monitoring area exists (within transfers page)
- ADMIN-08 shell: Order validation page exists with placeholder UI
- Admin dashboard provides overview of all admin sections
- Navigation is complete for full admin backoffice
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-backoffice-race-calendar/02-03-SUMMARY.md`
</output>
